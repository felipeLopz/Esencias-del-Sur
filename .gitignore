node_modules
.next
out
.env*.local
!.env.local.example
.vercel
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Respaldo local de las fotos de producto sin recortar (14MB, no va al repo)
_fotos-originales/
_fotos-recortadas/

# Skills de Neon que instaló el CLI de Vercel al provisionar la base
# (no los pedimos y la app no los usa).
.agents/
.claude/skills/
skills-lock.json
